import { defineConfig } from 'astro/config';

// If your site lives at https://username.github.io/SeglaMedFriendship/
// keep base as-is. If you use a custom domain, remove the base line.
export default defineConfig({
  site: 'https://pauwntus.github.io',
  base: '/SeglaMedFriendship',
  output: 'static',
});
